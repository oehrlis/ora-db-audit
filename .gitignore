# OS / Editor
**/.DS_Store
Thumbs.db
.vscode/*.code-workspace
.idea/

# Python / tooling
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/

# Archives
*.zip
*.tar
*.tgz
*.tar.gz

# Logs
*.log
/logs/

# Audit bundle output - contains real customer audit-trail data, keep LOCAL
audit_bundle/
*.mapping.json
*.anon.log
**/*.anon/

# Build artefacts
dist/*
!dist/.gitkeep
build/
artefacts/*.pdf

# Local workspace - extracted bundles, dev experiments
workspace/*
!workspace/.gitkeep

# Internal planning / session history - not for public repo
docs/history/

# Claude Code agent worktrees - transient sub-agent state
.claude/worktrees/

# Compliance source documents - kept locally for development reference
# (CIS Benchmarks, DISA STIG, Oracle Best Practice whitepaper). These are
# copyrighted upstream documents; download separately rather than mirror
# them in the OSS repo. See docs/compliance-mapping.md for source URLs.
references/

# AI toolkit symlinks - managed by aitk/init-repo.sh, not committed
.claude/commands/idea-capture.md
.claude/commands/idea-sparring.md
.claude/commands/repo-review.md
.claude/commands/security-check.md
.claude/commands/update-docs.md
.claude/rules/
.claude/skills/
.github/copilot-instructions.md
# aitk-managed - do not commit (deployed from ai-toolkit)
# A project-own file under these paths needs an explicit `git add -f`.
.claude/commands/
.claude/agents/
